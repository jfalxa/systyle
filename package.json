{
  "name": "moulinettes",
  "version": "0.0.0",
  "description": "mono repo of moulinette",
  "author": "",
  "license": "ISC",
  "repository": "github:jfalxa/systyle",
  "scripts": {
    "prep": "lerna clean && lerna bootstrap --hoist",
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "jest": "^24.1.0",
    "lerna": "^3.13.0",
    "ts-jest": "^24.0.0"
  }
}
